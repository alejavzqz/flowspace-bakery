%h3= @oven.name

.cookie-info
  Cookie in oven:
  - if @oven.cookies.exists?
    = render 'oven_cookies'
  - else
    None

%br

= link_to "Prepare Cookie", new_oven_cookies_path(@oven), class: 'button'
